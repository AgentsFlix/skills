{
  "name": "agentsflix-site",
  "private": true,
  "type": "module",
  "description": "Página do AgentsFlix (estática) + funções da API na Vercel: login Supabase, cobrança Stripe, vídeo Bunny.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "node scripts/build-identifier.mjs",
    "build:icons": "node design-system/build-icons.mjs",
    "check:icons": "node design-system/build-icons.mjs --check"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.117.1",
    "stripe": "^18.0.0"
  },
  "devDependencies": {
    "@tabler/icons": "3.47.0"
  }
}
