# Jornada visual da marca

Comparação local em Chrome, 9 de setembro de 2026. Capturas em 1440, 768 e 390 px. `before-*` mostra a interface anterior; `after-*` mostra as escolhas visuais, entrada e entrega esperada. `styles-1440` e `bank-390` mostram o banco; `stage-9-1440` mostra a rotina.

Validação: nove etapas nas três larguras, nenhuma opção inicial, cópia e fallback manual, retorno, retomada, dois negócios isolados, revisão de dependências, identidade visual, estática/carrossel, piloto e pasta declarada. Editor: 12 composições × 12 estilos, campos, estrutura/exemplo, slides, download ZIP válido, SVGs parseáveis e dentro do canvas, referência nos prompts e Escape. Sem erros de console ou overflow horizontal.

As fontes do mockup são exemplos editáveis. Não representam conteúdo aprovado nem verificam documentos no Drive. Testes externos de execução de skills continuam separados da interface.
